/**
 * Implement the five exports according to challenge/TASK.md.
 * Keep this module free of DOM and browser-only APIs so Node tests can import it.
 */

export function normalizeEvents(_rawEvents) {
  throw new Error("TODO: normalizeEvents");
}

export function reduceIncidentEvents(_validEvents, _atMs) {
  throw new Error("TODO: reduceIncidentEvents");
}

export function applyFilters(_incidents, _filters) {
  throw new Error("TODO: applyFilters");
}

export function calculateMetrics(_incidents) {
  throw new Error("TODO: calculateMetrics");
}

export function buildDashboard(_rawEvents, _filters, _atMs) {
  throw new Error("TODO: buildDashboard");
}
